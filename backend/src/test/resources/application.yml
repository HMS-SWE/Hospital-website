spring:
<<<<<<< HEAD
    application:
        name: backend
    main:
        web-application-type: servlet
    datasource:
        url: jdbc:h2:mem:testdb
        driver-class-name: org.h2.Driver
        username: sa
        password:
    jpa:
        hibernate:
            ddl-auto: create-drop
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.H2Dialect
    security:
        oauth2:
            client:
                registration:
                    google:
                        client-id: test-client-id
                        client-secret: test-client-secret
                        scope: openid, email, profile

app:
    jwt:
        secret: testsecretthatisfortycharacterslong1234567890
        expiration: 3600000
=======
  application:
    name: backend
  main:
    web-application-type: servlet
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test-client-id
            client-secret: test-client-secret
            scope: openid, email, profile

app:
  jwt:
    secret: testsecretthatisfortycharacterslong1234567890
    expiration: 3600000
>>>>>>> 94271e2 (added the resources packeges in test and added h2 dependency in pom.xml)
